#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int chercher (char ch[] , char c)
 {
int pos=-1;
int i;
 int a=strlen(ch);
for(i=a;i>-1;i--)
{
    if(ch[i]==c)
    {
        pos=i;
        i=-1;
    }
}
return pos ;
}
int search_all(char ch[] , char c,int pos[])
{

int i;
int j=-1;
 int a=strlen(ch);
for(i=0;i<a;i++)
{
    if(ch[i]==c)
    {
        j++;
        pos[j]=i;
        
    }
}
return j;
}
void delete_all(char ch[] , char c)
{
    int i,j;
    for(i=0;i<strlen(ch);i++)
    {
       if(ch[i]==c)
       {
          for(j=i;j<strlen(ch);j++)
             ch[j]=ch[j+1];
          i--;
       }
    }
    puts(ch);
}
int nb_Occ(char ch[] , char c)
{
    int occ=0;
int i;
 int a=strlen(ch);
for(i=0;i<a;i++)
{
    if(ch[i]==c)
    {
        occ++;
    }
}
return occ ;
}

int main ()
{   char c;
    char ch[50];
    int pos,n,i,occ;
    int tab[50];
 printf("donner le caractere a chercher \n");
scanf("%c",&c);
printf("donner votre chaine  \n");
scanf ("%s",ch);

 
pos=chercher(ch,c);
printf("%d\n",pos);
if(pos==-1)
{printf("ma fammech\n");}
else
printf("mawjoud w position hiya %d \n",pos+1);
n=search_all(ch ,c,tab);
printf("mawjoud w positions hiya\n");
for(i=0;i<n+1;i++)
{
    printf("%d\n",tab[i]);
}
 occ=nb_Occ(ch ,c);
 printf("occ est %d \n",occ);
 delete_all(ch,c);

    return 0;
}