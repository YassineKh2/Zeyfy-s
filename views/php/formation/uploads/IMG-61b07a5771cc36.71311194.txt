#include <stdio.h>
#include <stdlib.h>
#include <string.h>
typedef struct eleveur {
int cin;
int nbr_br;
int nbr_be;
int nbr_ag;
}eleveur;
void ajouter(eleveur tab[],int n)
{
    int i;
    for(i=0;i<n;i++)
    {
        printf("eleveur numero %d\n",i+1);
        printf("cin:\n");
        scanf("%d",&tab[i].cin);
        printf("nbr_br:\n");
        scanf("%d",&tab[i].nbr_br);
        printf("nbr_be:\n");
        scanf("%d",&tab[i].nbr_be);
        printf("nbr_ag:\n");
        scanf("%d",&tab[i].nbr_ag);
    }
}
void nbr_por(eleveur tab[],int n,int tab1[],float tab2[])
{
    int i;
    float r;
   for(i=0;i<n;i++)
   {
       tab1[i]=tab[i].nbr_br+tab[i].nbr_be+tab[i].nbr_ag;
       r=(float) tab[i].nbr_ag/tab1[i];
       tab2[i]= r*100;
   }
   for(i=0;i<n;i++)
   {
       printf("%d|",tab1[i]);
   }
   printf("\n");
      for(i=0;i<n;i++)
   {
       printf("%.2f %%|",tab2[i]);
   }
}
float nbrApourE(int tab1[],int n)
{
    int i;
    int somme=0;
    float nbr;
    for(i=0;i<n;i++)
    {
        somme+=tab1[i];
    }
    nbr=(float) somme/n;
    return nbr;
}
void chercher(int tab1[],eleveur tab[],int n)
{
    int i;
    int max=tab1[0];
    int cin;
    for(i=1;i<n;i++)
    {
        if(tab1[i]>max)
        { max=tab1[i];
            cin=tab[i].cin;
        }
    }
    printf("cin est %d et nbr animaux est %d\n",cin,max);
}
int main ()
{   
eleveur tab[50];
int tab1[50];
float tab2[50];
int n;
float nbr;
printf("donner la taille\n");
scanf("%d",&n);
ajouter(tab,n);
nbr_por(tab,n,tab1,tab2);
printf("\n");
nbr=nbrApourE(tab1,n);
printf("%.2f",nbr);
printf("\n");
chercher(tab1,tab,n);

    return 0;
}